<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.balanceeat.demo.domain.diet.mapper.DietMapper">
    <insert id="insert" parameterType="com.balanceeat.demo.domain.diet.entity.Diet">
        INSERT INTO diets (
            user_id,
            nutrition_id,
            diet_date,
            meal_type,
            meal_time,
            amount,
            note
        ) VALUES (
            #{userId},
            #{nutritionId},
            #{dietDate},
            #{mealType},
            #{mealTime},
            #{amount},
            #{note}
        )
    </insert>

    <update id="update" parameterType="com.balanceeat.demo.domain.diet.entity.Diet">
        UPDATE diets
        SET
            amount = #{amount},
            note = #{note},
            meal_type = #{mealType},
            meal_time = #{mealTime}
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM diets WHERE id = #{id}
    </delete>

    <select id="findByDate" resultType="com.balanceeat.demo.domain.diet.entity.Diet">
        SELECT 
            id,
            user_id as userId,
            nutrition_id as nutritionId,
            diet_date as dietDate,
            meal_type as mealType,
            meal_time as mealTime,
            amount,
            note
        FROM diets
        WHERE user_id = #{userId}
        AND diet_date = #{date}
        ORDER BY meal_type, id
    </select>

    <select id="findById" resultType="com.balanceeat.demo.domain.diet.entity.Diet">
        SELECT 
            id,
            user_id as userId,
            nutrition_id as nutritionId,
            diet_date as dietDate,
            meal_type as mealType,
            meal_time as mealTime,
            amount,
            note
        FROM diets
        WHERE id = #{id}
    </select>

    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO diets (
            user_id,
            nutrition_id,
            food_name,
            diet_date,
            meal_type,
            meal_time,
            amount,
            note
        ) VALUES 
        <foreach collection="list" item="diet" separator=",">
            (
                #{diet.userId},
                #{diet.nutritionId},
                #{diet.foodName},
                #{diet.dietDate},
                #{diet.mealType},
                #{diet.mealTime},
                #{diet.amount},
                #{diet.note}
            )
        </foreach>
    </insert>
</mapper> 