<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.balanceeat.demo.domain.diet.mapper.DietSummaryMapper">
    
    <select id="findByDateAndUserId" resultType="com.balanceeat.demo.domain.diet.entity.DietSummary">
        SELECT 
            id,
            user_id as userId,
            summary_date as summaryDate,
            
            -- 아침 식사 영양 정보
            breakfast_calories as breakfastCalories,
            
            -- 점심 식사 영양 정보
            lunch_calories as lunchCalories,
            
            -- 저녁 식사 영양 정보
            dinner_calories as dinnerCalories,
            
            -- 간식 영양 정보
            snack_calories as snackCalories,
            
            -- 야식 영양 정보
            night_calories as nightCalories,
            
            -- 일일 총 영양 정보
            total_calories as totalCalories,
            
            created_at as createdAt,
            updated_at as updatedAt
        FROM diet_summaries
        WHERE summary_date = #{date} AND user_id = #{userId}
    </select>
    
    <select id="findByDateRange" resultType="com.balanceeat.demo.domain.diet.entity.DietSummary">
        SELECT 
            id,
            user_id as userId,
            summary_date as summaryDate,
            
            -- 아침 식사 영양 정보
            breakfast_calories as breakfastCalories,
            
            -- 점심 식사 영양 정보
            lunch_calories as lunchCalories,
            
            -- 저녁 식사 영양 정보
            dinner_calories as dinnerCalories,
            
            -- 간식 영양 정보
            snack_calories as snackCalories,
            
            -- 야식 영양 정보
            night_calories as nightCalories,
            
            -- 일일 총 영양 정보
            total_calories as totalCalories,
            
            created_at as createdAt,
            updated_at as updatedAt
        FROM diet_summaries
        WHERE user_id = #{userId}
        AND summary_date BETWEEN #{start} AND #{end}
        ORDER BY summary_date
    </select>
    
    <insert id="insert" parameterType="com.balanceeat.demo.domain.diet.entity.DietSummary">
        INSERT INTO diet_summaries (
            user_id, 
            summary_date,
            
            -- 아침 식사 영양 정보
            breakfast_calories,
            
            -- 점심 식사 영양 정보
            lunch_calories,
            
            -- 저녁 식사 영양 정보
            dinner_calories,
            
            -- 간식 영양 정보
            snack_calories,
            
            -- 야식 영양 정보
            night_calories,
            
            -- 일일 총 영양 정보
            total_calories,
            
            created_at,
            updated_at
        ) VALUES (
            #{userId}, 
            #{summaryDate},
            
            -- 아침 식사 영양 정보
            #{breakfastCalories},
            
            -- 점심 식사 영양 정보
            #{lunchCalories},
            
            -- 저녁 식사 영양 정보
            #{dinnerCalories},
            
            -- 간식 영양 정보
            #{snackCalories},
            
            -- 야식 영양 정보
            #{nightCalories},
            
            -- 일일 총 영양 정보
            #{totalCalories},
            
            NOW(),
            NOW()
        )
    </insert>
    
    <update id="update" parameterType="com.balanceeat.demo.domain.diet.entity.DietSummary">
        UPDATE diet_summaries
        SET
            -- 아침 식사 영양 정보
            breakfast_calories = #{breakfastCalories},
            
            -- 점심 식사 영양 정보
            lunch_calories = #{lunchCalories},
            
            -- 저녁 식사 영양 정보
            dinner_calories = #{dinnerCalories},
            
            -- 간식 영양 정보
            snack_calories = #{snackCalories},
            
            -- 야식 영양 정보
            night_calories = #{nightCalories},
            
            -- 일일 총 영양 정보
            total_calories = #{totalCalories},
            
            updated_at = NOW()
        WHERE id = #{id}
    </update>
</mapper> 