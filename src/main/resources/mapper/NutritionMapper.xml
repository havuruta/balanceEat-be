<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.balanceeat.demo.domain.nutrition.mapper.NutritionMapper">
    <resultMap id="NutritionResultMap" type="com.balanceeat.demo.domain.nutrition.entity.Nutrition">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="calories" column="calories"/>
        <result property="protein" column="protein"/>
        <result property="carbohydrates" column="carbohydrates"/>
        <result property="fat" column="fat"/>
        <result property="category" column="category" typeHandler="com.balanceeat.demo.domain.nutrition.entity.FoodCategoryTypeHandler"/>
    </resultMap>

    <select id="selectById" resultMap="NutritionResultMap">
        SELECT id, name, calories, protein, carbohydrates, fat, category
        FROM nutrition
        WHERE id = #{id}
    </select>

    <select id="selectAll" resultMap="NutritionResultMap">
        SELECT id, name, calories, protein, carbohydrates, fat, category
        FROM nutrition
        ORDER BY id
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM nutrition
    </select>
    
    <select id="searchByCategory" resultMap="NutritionResultMap">
        SELECT id, name, calories, protein, carbohydrates, fat, category
        FROM nutrition
        WHERE category = #{category}
        ORDER BY id
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countByCategory" resultType="long">
        SELECT COUNT(*)
        FROM nutrition
        WHERE category = #{category}
    </select>

    <select id="searchByName" resultMap="NutritionResultMap">
        SELECT id, name, calories, protein, carbohydrates, fat, category
        FROM nutrition
        WHERE name LIKE CONCAT('%', #{name}, '%')
        ORDER BY id
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countByName" resultType="long">
        SELECT COUNT(*)
        FROM nutrition
        WHERE name LIKE CONCAT('%', #{name}, '%')
    </select>

    <select id="findByIds" resultMap="NutritionResultMap">
        SELECT id, name, calories, protein, carbohydrates, fat, category
        FROM nutrition
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        ORDER BY id
    </select>
</mapper> 